services:
  session-server:
    build:
      context: ../../../src/server
      dockerfile: ../../infra/docker/server.Dockerfile
      args:
        BUILD_MODE: dev
    container_name: jwp-session-server
    ports:
      - "${SESSION_SERVER_PORT:-3000}:3000"
    restart: unless-stopped
    environment:
      - HOST=${HOST:-0.0.0.0}
      - PORT=${PORT:-3000}
      - ALLOWED_ORIGINS=${ALLOWED_ORIGINS:-http://localhost:8096,https://localhost:8096}
      - JWT_SECRET=${JWT_SECRET:-}
      - JWT_AUDIENCE=${JWT_AUDIENCE:-JellyWatchParty}
      - JWT_ISSUER=${JWT_ISSUER:-Jellyfin}
    deploy:
      resources:
        limits:
          memory: 256M
          cpus: '0.5'
        reservations:
          memory: 64M
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:3000/health"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 5s

  jellyfin-dev:
    image: jellyfin/jellyfin:10.11.11
    container_name: jellyfin-dev
    environment:
      - PUID=${UID:-1000}
      - PGID=${GID:-1000}
    depends_on:
      - session-server
    ports:
      - "${JELLYFIN_PORT:-8096}:8096"
    volumes:
      - ./config:/config
      - jellyfin-cache:/cache
      - ${MEDIA_DIR:-${HOME}/Videos/Movies}:/media/Movies:ro
      - ../../../src/clients/jellyfin-web:/jellyfin/jellyfin-web/plugins/jellywatchparty:ro
      - ../../../src/plugins/jellyfin/JellyWatchParty/dist:/config/plugins/JellyWatchParty
      - ./scripts/jellyfin-entrypoint.sh:/jellyfin-entrypoint.sh:ro
    restart: unless-stopped
    entrypoint: /jellyfin-entrypoint.sh

  plugin-builder:
    image: mcr.microsoft.com/dotnet/sdk:9.0
    working_dir: /workspace/src/plugins/jellyfin/JellyWatchParty
    network_mode: host
    profiles:
      - tools
    volumes:
      - ../../..:/workspace
    environment:
      - HOME=/tmp
      - DOTNET_CLI_HOME=/tmp/.dotnet
    command: dotnet publish -c Release -o dist JellyWatchPartyPlugin.csproj

volumes:
  jellyfin-cache:
